import React from 'react';

const Text = (props) => {
    return (
        <text id="logoText" x="280" y="490" fontSize={props.fontSize} fill="black" textLength={props.textLength}>osh Miccolo</text> 
    );
};

export default Text;